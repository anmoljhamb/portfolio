import React from "react";
import "./Navbar.scss";

const Navbar = () => {
    return (
        <section className="flex">
            <div className="navbar">Navbar</div>
        </section>
    );
};

export default Navbar;
